# QR-Code-Generator-Python-
simple terminal-based QR code generator built with Python. The program allows users to input text or URLs and generates a QR code image (PNG) using the qrcode and Pillow libraries. This project demonstrates external library usage, input validation, and file generation.
